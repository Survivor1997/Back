/*let country = "Burundi";
let continent = "Africa";
let population = 14359635;
console.log(country, continent, population);

let markMass = 78;
let markHeight = 1.69;
let johnMass = 92;
let johnHeight = 1.95;
function bmi(height, mass) {
  let bmi = mass / height ** 2;
  return bmi;
}
let bmiMark = bmi(markHeight, markMass);
let bmiJohn = bmi(johnHeight, johnMass);
console.log(bmiMark, bmiJohn);
if (bmiMark > bmiJohn) {
  console.log(`Mark's BMI (${bmiMark}) is higher than John's (${bmiJohn})`);
} else {
  console.log(`John's BMI (${bmiJohn}) is higher than Mark's (${bmiMark})`);
}

let dolphinScore;
let koalaScore;
let dolphinAveScore = (96 + 108 + 89) / 3;
let koalaAveScore = (88 + 91 + 110) / 3;
if ((dolphinAveScore > koalaAveScore) & 100) {
  console.log(`Dolphin wins with a high score of ${dolphinAveScore} 🎉 `);
} else if ((koalaAveScore = dolphinAveScore & 100)) {
  console.log(`there is a draw nobody wins .`);
} else {
  console.log(`koala wins with a high score of ${koalaAveScore} 😁🎉🎉`);
}
*/
let bill = 275;
let tip;
bill >= 50 && bill <= 300 ? tip=bill*1.5
 console.log(`the bill was ${bill}, the tip was ${tip}`):tip=bill*0.2
 console.log(`the bill was ${bill}, the tip was ${tip}`);